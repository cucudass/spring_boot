<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Add Input Dynamically with Numbering</title>
<style>
    /* Optional: Some basic styling */
    .container {
        margin-top: 20px;
    }
    .input-group {
        margin-bottom: 10px;
    }
</style>
</head>

<body>
    <form>
        <div class="container">
			<hr>
            <div id="inputContainer">
                <h3>자격증</h3>
                <div class="input-group">
                    입력란: <input type="text" name="inputField1" placeholder="자격증명">
                </div>
            </div>
			<hr>
            <button type="button" id="addInputBtn">Add Input</button>
        </div>
		<br>
        <div>
            <button type="button" onclick="cntchk()">숫자 체크</button>&nbsp;&nbsp;
			<button type="button" onclick="valueshow()">입력값 출력</button>
        </div>
    </form>
	<script>
		// JavaScript to add input field dynamically with numbering
		var inputCount = 1; // Initialize input count

		document.getElementById('addInputBtn').addEventListener('click', function() {
			var inputContainer = document.getElementById('inputContainer');

			// Increment input count
			inputCount++;

			// Create a new input group
			var newInputGroup = document.createElement('div');
			newInputGroup.className = 'input-group';

			// Create a new input field with numbered name and placeholder
			var newInput = document.createElement('input');
			newInput.type = 'text';
			newInput.name = 'inputField' + inputCount; // Use incremented count for name
			newInput.placeholder = '자격증명';
			var labelText = document.createTextNode('입력란: ');
			
			// Append the new input field to the input group
			newInputGroup.appendChild(labelText);
			newInputGroup.appendChild(newInput);

			// Append the new input group to the container
			inputContainer.appendChild(newInputGroup);
		});

		function cntchk(){
			alert("inputCount: "+inputCount);
		}

		function valueshow() {
			for(var i=0; i<inputCount; i++) {
				var value = document.getElementsByName("inputField"+(i+1))[0].value;
				alert(value);
			}
		}
	</script>
</body>
</html>
